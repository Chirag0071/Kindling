# The backend API URL. This is inlined into the JS bundle at BUILD TIME
# (Next.js requirement for NEXT_PUBLIC_ vars) — so on Vercel, set this in
# Project Settings > Environment Variables BEFORE deploying, not after.
NEXT_PUBLIC_API_URL=http://localhost:8000
